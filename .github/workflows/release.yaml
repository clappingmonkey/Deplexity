name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      dry-run:
        description: Run CI only, skip release-please and publish
        type: boolean
        default: false

permissions:
  contents: write
  pull-requests: write

jobs:
  ci:
    name: CI
    uses: ./.github/workflows/bazel-ci.yaml
    with:
      run-tests: true
      run-build: true

  release-please:
    name: Release Please
    runs-on: ubuntu-latest
    needs: [ci]
    if: ${{ !inputs.dry-run }}
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
    steps:
      - uses: tibdex/github-app-token@3beb63f4bd073e61482598c45c71c1019b59b73a # v2
        id: app-token
        with:
          app_id: ${{ secrets.APP_ID }}
          private_key: ${{ secrets.APP_PRIVATE_KEY }}

      - uses: googleapis/release-please-action@5c625bfb5d1ff62eadeeb3772007f7f66fdcf071 # v4
        id: release
        with:
          config-file: .github/release-please-config.json
          manifest-file: .github/release-please-manifest.json
          token: ${{ steps.app-token.outputs.token }}

  publish:
    name: Publish
    needs: [release-please]
    if: needs.release-please.outputs.release_created == 'true'
    uses: ./.github/workflows/bazel-ci.yaml
    with:
      publish-platforms: '[{"config":"linux_amd64","output":"deplexity-linux-amd64","ext":""},{"config":"linux_arm64","output":"deplexity-linux-arm64","ext":""},{"config":"darwin_amd64","output":"deplexity-darwin-amd64","ext":""},{"config":"darwin_arm64","output":"deplexity-darwin-arm64","ext":""},{"config":"windows_amd64","output":"deplexity-windows-amd64.exe","ext":".exe"}]'
      tag-name: ${{ needs.release-please.outputs.tag_name }}

  upload-release:
    name: Upload to Release
    runs-on: ubuntu-latest
    needs: [release-please, publish]
    if: needs.release-please.outputs.release_created == 'true'
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: dist-${{ needs.release-please.outputs.tag_name }}
          path: dist

      - name: Upload binaries to release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          tag_name: ${{ needs.release-please.outputs.tag_name }}
          files: dist/*
